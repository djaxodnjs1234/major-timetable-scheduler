# Revert Unapproved Main Merge

1. Revert merge commit `8c79df0` without rewriting branch history.
   Verify: the working tree is clean and the revert commit is created.
2. Compare each future conflict file before editing it.
   Verify: present the proposed resolution to the user and wait for approval before applying it.
