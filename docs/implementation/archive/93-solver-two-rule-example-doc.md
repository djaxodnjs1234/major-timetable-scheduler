# Solver Two Rule Example Document

1. Create a standalone Korean markdown document explaining solver behavior with two simple rules.
   - Verify: the document includes variables, two rules, a step-by-step solving example, and project mapping.
2. Keep the explanation scoped to the user's latest example.
   - Verify: no unrelated migration or UI design content is added.
3. Verify the document can be read and archive this plan.
   - Verify: search finds the document title and the plan is moved to `docs/implementation/archive/`.
