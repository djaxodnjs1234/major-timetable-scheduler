# WPF Detail Empty Text and Preview Layout

1. Show empty detail summaries.
   - Verify: course and professor detail rows display : 없음 when optional values are empty and show actual content when present.

2. Hide select-all until editing.
   - Verify: unavailable-room select-all button is hidden in read-only mode and visible only while editing a course.

3. Reduce preview solution list height.
   - Verify: timetable preview top solution list uses less vertical space without hiding controls.

4. Move grade legend upward.
   - Verify: every timetable places the grade color legend on the same row as the integrated/grade tabs.

5. Run focused WPF checks.
   - Verify: related ViewModel/WPF tests pass or any unrelated failures are documented.
