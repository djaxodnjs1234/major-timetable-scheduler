# Incomplete Edit Locations and Cancellation

1. Include the category and display name of every active editor in the IE-037 diagnostic. Verify the diagnostic lists course, professor, and room edits.
2. Add cancel operations for course groups, professors, and rooms that discard their edit copies without persisting changes. Verify each cancel restores the displayed persisted data and exits edit mode.
3. Add WPF cancel buttons for every editable item type. Verify bindings and run the focused tests and WPF build before archiving this plan.
