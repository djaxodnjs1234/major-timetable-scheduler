# Document WPF Hard and Soft Constraints

1. Inspect the WPF solver and validation sources for the active hard constraints, soft constraints, and their numeric settings.  
   Verify: every documented rule is traceable to a WPF source or configuration value.
2. Create one Korean Markdown reference in `docs/` that lists hard constraints and soft constraints, with a settings-and-numbers subsection at the end of every rule.  
   Verify: the document contains no unrelated usage or implementation instructions.
3. Review the document against the source values, then archive this plan.  
   Verify: documented numeric values match the WPF code and no active plan remains.
