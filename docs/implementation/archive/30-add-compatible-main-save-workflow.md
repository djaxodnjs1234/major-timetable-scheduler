# Add Compatible Main Save Workflow

1. Carry saved timetable identity from input and selection into manual editing.
   Verify: editing an existing saved timetable targets its original record.
2. Add overwrite and save-copy commands without changing existing manual edit validation.
   Verify: focused ViewModel tests and WPF build pass.
3. Review remaining main-only changes for compatibility, then archive this plan.
   Verify: no current-branch user feature is removed.
