# Unavailable Room Quick Selection

1. Replace the unavailable-room "select all" action with lab, non-lab, and clear actions based on the existing room classification. Verify each action updates only the intended checked items.
2. Preserve the existing fixed-room and unavailable-room exclusivity behavior. Verify it through the DataInput WPF tests.
3. Run the focused WPF tests and WPF build. Archive this plan after successful verification.
