Write project architecture explanation documents and diagrams.

## Goal

Create two shareable Korean Markdown documents:

1. Internal project structure document, with detailed coverage of the timetable auto-generation engine.
2. Project overview document, focused on development components, with an image for each component.

## Steps and verification

1. Gather context from WPF/prototype source and existing docs.
   - Verify: identify actual modules, dependency flow, solver pipeline, and key source files.
2. Draft the internal structure document.
   - Verify: includes project layers, data flow, automatic generation engine structure, constraints/scoring, and diagrams.
3. Draft the project overview document.
   - Verify: includes development components and one diagram per component.
4. Review final Markdown files.
   - Verify: diagrams are valid SVG files, local file references are readable, and wording matches the codebase.
5. Archive this plan after completion.
   - Verify: move this file to `docs/implementation/archive/22-project-architecture-docs.md`.
