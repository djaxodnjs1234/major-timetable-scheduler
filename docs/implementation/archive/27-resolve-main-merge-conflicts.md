# Resolve Main Merge Conflicts

1. Review each unmerged file from the active `main` merge and identify the behavior contributed by both `logic_add` and `main`. Verify every conflict hunk has a clear combined resolution.
2. Resolve the six conflicted WPF files without discarding either side's relevant changes. Verify Git reports no unmerged paths or conflict markers.
3. Run focused WPF tests and build the merged result. Archive this plan after verification.
