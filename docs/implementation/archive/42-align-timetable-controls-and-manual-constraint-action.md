# Align timetable controls and manual constraint action

1. Place zoom controls on the right side of the timetable header in selection, results, and manual editing views.
   - Verify: all three views retain working zoom controls in the right-side header area.
2. Move the manual editor's constraint-edit action to the right side of its toolbar and apply the green action style.
   - Verify: the button invokes the existing command and remains visually distinct from neutral actions.
3. Build and run focused view-model and zoom tests, then archive this plan.
   - Verify: WPF build and focused tests pass.
