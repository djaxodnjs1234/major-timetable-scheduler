# Restructure Constraint Settings Reference

1. Add compact HC and SC summary tables at the top of the WPF constraint settings document.  
   Verify: every documented HC and SC appears once in its corresponding summary table.
2. Move the existing detailed condition explanations below the summary tables without changing their settings values.  
   Verify: each condition still ends with its detailed settings-and-numbers subsection.
3. Review heading order and archive this plan.  
   Verify: summary tables precede all detailed explanations and no active plan remains.
