# Manual Edit Snapshot Example

1. Create a concrete JSON example of a saved manual-edit snapshot.
   - Verify: the file shows local courses, professors, rooms, policy snapshots, assignments, and staged blocks in one saved payload.
2. Validate the JSON syntax.
   - Verify: the example parses successfully as JSON.
3. Move this plan to `docs/implementation/archive/`.
   - Verify: the plan file is archived after the example is complete.
